{{! Sheet Header }}
<header class='sheet-header'>
  <img
    class='profile-img'
    src='{{actor.img}}'
    data-edit='img'
    data-action='onEditImage'
    title='{{actor.name}}'
    height='300'
    width='200'
  />
  <div class='header-fields'>
    <div class='document-name'>
      <input
        name='name'
        type='text'
        value='{{actor.name}}'
        placeholder='Name'
      />
    </div>
    {{! The grid classes are defined in scss/global/_grid.scss. To use,
      use both the "grid" and "grid-Ncol" class where "N" can be any number
      from 1 to 12 and will create that number of columns.  }}
    <div class='resources grid grid-3col'>

      {{! "flex-group-center" is also defined in the _grid.scss file
        and it will add a small amount of padding, a border, and will
        center all of its child elements content and text. }}
      <div class='resource flex-group-center'>
        <label for='system.structure_point.value' class='resource-label'>
          {{localize 'PJSD.Actor.base.FIELDS.structure_points.value.label'}}
        </label>
        <div class='resource-content flexrow flex-center flex-between'>
          <input
            type='text'
            name='system.structure_points.value'
            value='{{system.structure_points.value}}'
            data-dtype='Number'
          />
          <span> / </span>
          <input
            type='text'
            name='system.structure_points.max'
            value='{{system.attributes.structure_points.max}}'
            data-dtype='Number'
          />
        </div>
      </div>

      <div class='resource flex-group-center'>
        <label for='system.blood_points.value' class='resource-label'>
          {{localize 'PJSD.Actor.base.FIELDS.blood_points.value.label'}}
        </label>
        <div class='resource-content flexrow flex-center flex-between'>
          <input
            type='text'
            name='system.blood_points.value'
            value='{{system.blood_points.value}}'
            data-dtype='Number'
          />
          <span> / </span>
          <input
            type='text'
            name='system.power.max'
            value='{{system.attributes.structure_points.max}}'
            data-dtype='Number'
          />
        </div>
      </div>

      <div class='resource flex-group-center'>
        {{#if (eq actor.type 'character')}}
          <label class='resource-label'>
            {{localize 'PJSD.Actor.Character.FIELDS.attributes.mass.value.label'}}
          </label>
          <div class='resource-content flexrow flex-center flex-between'>
            <select
              
              name='system.attributes.mass.value'
            >
              {{selectOptions choices selected=value localize=true}}
              data-dtype='Number'
            {{#each choices as |choice|}}
                <option value='{{choice.value}}'>{{choice.label}}</option>
              {{/each}}
            </select>
              
          </div>
        {{/if}}
        {{#if (eq actor.type 'npc')}}
          <label class='resource-label'>
            {{localize 'PJSD.Actor.NPC.FIELDS.cr.label'}} / {{localize 'PJSD.Actor.NPC.xp.label'}}
          </label>
          <div class='resource-content flexrow flex-center flex-between'>
            <input
              type='text'
              name='system.cr'
              value='{{system.cr}}'
              data-dtype='Number'
            />
            <span></span>
            <input
              type='text'
              disabled='true'
              name='system.xp'
              value='{{system.xp}}'
              data-dtype='Number'
            />
          </div>
        {{/if}}
      </div>
    </div>
  </div>
</header>